import { CanEditAndDeletePipe } from './can-edit-and-delete.pipe';

describe('CanEditAndDeletePipe', () => {
  it('create an instance', () => {
    const pipe = new CanEditAndDeletePipe();
    expect(pipe).toBeTruthy();
  });
});
